function sayHello(name){
    var helloWorld = '';
    if(name.length < 1)
        varWithWrongName = `Hello Sentry! You got it right!!! This should be removed-> ${varWithWrongName}`;
    else
        helloWorld = `Hello ${name}!`;
    showHello(helloWorld);
}
